model_size: medium
language: auto

silence:
  threshold: 0.02
  min_duration: 0.30

jumpcut:
  enabled: true            # TURN ON
  remove_if_silence_longer_than: 1.0
  pad_before: 0.10
  pad_after: 0.25

pad:
  before: 0.10
  after: 0.30

video:
  min_clip_sec: 0.80
  merge_gap_sec: 0.40
